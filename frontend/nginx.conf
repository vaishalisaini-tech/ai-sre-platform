# frontend/nginx.conf
server {
  listen 80;

  # Serve the React app
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }

  # Forward /api/* to the backend service (Kubernetes DNS name "backend")
  location /api/ {
    proxy_pass http://backend:8000/;
  }
}

